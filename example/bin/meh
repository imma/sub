#!/usr/bin/env bash

PATH="$(unset CDPATH; cd -P -- "$(dirname -- "$BASH_SOURCE")/../.." && pwd -P)/exec:$PATH"

export MEH=1

function main {
  ps axu | head
  echo "$@"
  echo "$MEH"
}

source sub "$0" "$@"
