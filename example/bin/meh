#!/usr/bin/env bash

export MEH=1

_shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"
PATH="$_shome/bin:$_shome/exec:$PATH"

function main {
  ps axu | head
  echo "$@"
  echo "$MEH"
}

source sub "$0" "$@"
