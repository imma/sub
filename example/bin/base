#!/usr/bin/env bash

_shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"
PATH="$_shome/bin:$_shome/exec:$PATH"

function main {
  df -klh
}

export SUB_MIDDLEWARE='base'
source sub "$BASH_SOURCE" "$@"
